name: Build and Release

on:
  push:
    branches:
      - main

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: pip install pyinstaller mediapipe opencv-python numpy
      - run: pyinstaller --noconfirm --onefile --windowed --add-data "utkucann.mp3:." focus-tracker.py
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mac-app
          path: dist/focus-tracker

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: pip install pyinstaller mediapipe opencv-python numpy
      - run: pyinstaller --noconfirm --onefile --windowed --add-data "utkucann.mp3;." focus-tracker.py
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist/focus-tracker.exe

  release:
    needs: [build-mac, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          prerelease: true
          files: |
            mac-app/focus-tracker
            windows-exe/focus-tracker.exe